import { createSlice } from "@reduxjs/toolkit";

const initialState = {
    doctors:[]
}

const appSlice = createSlice({
    name: "app",
    initialState,
    reducers: {
        
     }
})

export default appSlice.reducer;